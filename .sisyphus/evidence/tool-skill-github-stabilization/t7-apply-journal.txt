T7 apply journal verification

Test coverage added in .opencode/plugins/agent-toolkit.test.ts:
- issue_status compares journal file snapshot before/after and expects no change.
- issue_create_from_spec dryRun:true compares journal file snapshot before/after and expects no change.
- issue_create_from_spec dryRun:false compares journal file snapshot before/after, expects a change, and asserts exactly one journal entry with tag "applied".

Implementation behavior:
- handleIssueCreateFromSpec appends to journal only when dryRun is false.
- handleIssueStatus delegates with dryRun:true, so it remains journal-free.
- PR watch/event lifecycle code paths were not modified.

Verification command:
- bun test ./.opencode/plugins/agent-toolkit.test.ts → 77 pass, 0 fail
