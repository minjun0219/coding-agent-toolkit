{
  "$schema": "https://json.schemastore.org/claude-code-plugin.json",
  "name": "agent-toolkit",
  "version": "0.3.0",
  "description": "agent-toolkit Claude Code plugin (Bun runtime). 7 openapi_* tools — cache-first OpenAPI / Swagger spec inspection, endpoint search, tag list. Subset packages (mysql / notion / spec-pact / pr-watch) ship separately.",
  "author": {
    "name": "Minjun Kim"
  },
  "license": "MIT",
  "keywords": ["openapi", "swagger", "agent-toolkit", "bun", "mcp"]
}
