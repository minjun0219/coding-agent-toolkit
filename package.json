{
  "name": "agent-toolkit",
  "version": "0.1.0",
  "description": "opencode plugin: Notion page cache + skill (Bun)",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.0.0"
  },
  "scripts": {
    "test": "bun test ./lib ./.opencode ./tools",
    "typecheck": "bun x tsc --noEmit -p tsconfig.json",
    "lint:comments": "bun run tools/check-comments.ts lib .opencode/plugins"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/bun": "^1.1.0"
  }
}
